generated automatically via types and reflection via manifest in every service dir

model files are hoisted based on their locations

codegen provides their glue.

when doing an install, sdk needs to be built first

sdk needs to be watched if working on new features
